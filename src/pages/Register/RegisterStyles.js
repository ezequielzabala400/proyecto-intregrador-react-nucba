import { styled } from "styled-components";
import { ButtonLogin, LoginForm, LoginWrapper, RegisterLink, RegisterText } from "../Login/LoginStyles";

export const RegisterWrapper = styled(LoginWrapper)`

`

export const RegisterForm = styled(LoginForm)`
`


export const RegisterBtn = styled(ButtonLogin)``

export const LoginText = styled(RegisterText)``

export const LoginLink = styled(RegisterLink)``

