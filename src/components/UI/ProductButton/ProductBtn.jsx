import React from 'react'
import { ProductButton } from './ProductBtnStyles'

const ProductBtn = ({ children }) => {
  return (
    <ProductButton>{children}</ProductButton>
  )
}

export default ProductBtn